2022 10 03

1712번 손익분기점 : https://www.acmicpc.net/problem/1712

#include <stdio.h>
	int main() {
		double A, B, C;
		double bep;

		scanf("%lf %lf %lf", &A, &B, &C);

		if (C <= B) {
			printf("-1");
		}
		else {
			bep = A / (C - B);
			printf("%lld", (long long)bep + 1);
		}

		return 0;
	}
